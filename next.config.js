/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.pexels.com",
      "www.pexels.com",
      "vitals.vercel-insights.com",
    ],
  },
};

module.exports = nextConfig;
